const {ccclass, property} = cc._decorator;

export module SYS {
    export var SH;      // window height
    export var SW;      // window width
    export var SH2;     // half window height
    export var SW2;     // half window width
}